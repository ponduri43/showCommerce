<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:staggered="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.samskrut.showcommerce.CategoryGridView" >

    <include
        android:id="@+id/app_bar"
        layout="@layout/app_bar" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/cardList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/app_bar"
        android:clipToPadding="false" />

    <LinearLayout
        android:id="@+id/startSessionll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/app_bar"
        android:layout_centerInParent="true"
        android:background="#99000000"
        android:gravity="center"
        android:orientation="vertical" >

        <Button
            android:id="@+id/startSession"
            android:layout_width="@dimen/sessionbox"
            android:layout_height="wrap_content"
            android:layout_margin="4dp"
            android:background="@color/darker_gray"
            android:padding="10dp"
            android:text="@string/with_seesion" />

        <Button
            android:id="@+id/startNormaly"
            android:layout_width="@dimen/sessionbox"
            android:layout_height="wrap_content"
            android:layout_margin="4dp"
            android:background="@color/darker_gray"
            android:padding="10dp"
            android:text="@string/without_session" />
    </LinearLayout>

</RelativeLayout>